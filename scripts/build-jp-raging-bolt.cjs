#!/usr/bin/env node
const path = require("path");
const fs = require("fs");
const cards = require(path.join(__dirname, "..", "dist", "cards-western.json"));

function findCard(name, setName, number) {
  const match = cards.find(c => {
    const cardName = c.names?.en || c.name || "";
    return cardName === name && c.set === setName && c.number === String(number);
  });
  if (!match) {
    console.error("WARNING: Could not find card: " + name + " | " + setName + " | " + number);
    return null;
  }
  return match;
}

function ptcgoLine(qty, card) {
  const name = card.names?.en || card.name || "???";
  return "* " + qty + " " + name + " " + card.set + " " + card.number;
}

const pokemon = [
  { qty: 3, name: "Raging Bolt ex", set: "Temporal Forces", num: "123" },
  { qty: 3, name: "Teal Mask Ogerpon ex", set: "Twilight Masquerade", num: "025" },
  { qty: 3, name: "Noctowl", set: "Stellar Crown", num: "115" },
  { qty: 3, name: "Hoothoot", set: "Stellar Crown", num: "114" },
  { qty: 2, name: "Fan Rotom", set: "Stellar Crown", num: "118" },
  { qty: 1, name: "Koraidon", set: "Surging Sparks", num: "116" },
  { qty: 1, name: "Raging Bolt", set: "Stellar Crown", num: "111" },
  { qty: 1, name: "Fezandipiti ex", set: "Shrouded Fable", num: "038" },
  { qty: 1, name: "Latias ex", set: "Surging Sparks", num: "076" },
  { qty: 1, name: "Ditto", set: "151", num: "132" },
  { qty: 1, name: "Squawkabilly ex", set: "Paldea Evolved", num: "169" },
];

const trainers = [
  { qty: 4, name: "Professor Sada" + String.fromCharCode(39) + "s Vitality", set: "Paradox Rift", num: "170" },
  { qty: 4, name: "Nest Ball", set: "Scarlet & Violet", num: "181" },
  { qty: 4, name: "Ultra Ball", set: "Scarlet & Violet", num: "196" },
  { qty: 3, name: "Earthen Vessel", set: "Paradox Rift", num: "163" },
  { qty: 3, name: "Night Stretcher", set: "Shrouded Fable", num: "061" },
  { qty: 2, name: "Boss" + String.fromCharCode(39) + "s Orders", set: "Paldea Evolved", num: "172" },
  { qty: 2, name: "Area Zero Underdepths", set: "Stellar Crown", num: "131" },
  { qty: 1, name: "Crispin", set: "Stellar Crown", num: "133" },
  { qty: 1, name: "Iono", set: "Paldea Evolved", num: "185" },
  { qty: 1, name: "Professor Turo" + String.fromCharCode(39) + "s Scenario", set: "Paradox Rift", num: "171" },
  { qty: 1, name: "Jamming Tower", set: "Twilight Masquerade", num: "153" },
  { qty: 1, name: "Energy Switch", set: "Scarlet & Violet", num: "173" },
  { qty: 1, name: "Prime Catcher", set: "Temporal Forces", num: "157" },
];

const energy = [
  { qty: 6, name: "Basic Grass Energy", set: "Scarlet & Violet Energies", num: "1" },
  { qty: 3, name: "Basic Fighting Energy", set: "Scarlet & Violet Energies", num: "6" },
  { qty: 3, name: "Basic Lightning Energy", set: "Scarlet & Violet Energies", num: "4" },
];

const deckLines = [];
let totalCards = 0;
const pokemonCount = pokemon.reduce((s, e) => s + e.qty, 0);
const trainerCount = trainers.reduce((s, e) => s + e.qty, 0);
const energyCount = energy.reduce((s, e) => s + e.qty, 0);

deckLines.push("****** Pokémon Trading Card Game Deck List ******");
deckLines.push("");
deckLines.push("##Pokémon - " + pokemonCount);
deckLines.push("");

for (const entry of pokemon) {
  const card = findCard(entry.name, entry.set, entry.num);
  if (card) { deckLines.push(ptcgoLine(entry.qty, card)); totalCards += entry.qty; }
}

deckLines.push("");
deckLines.push("##Trainer Cards - " + trainerCount);
deckLines.push("");

for (const entry of trainers) {
  const card = findCard(entry.name, entry.set, entry.num);
  if (card) { deckLines.push(ptcgoLine(entry.qty, card)); totalCards += entry.qty; }
}

deckLines.push("");
deckLines.push("##Energy - " + energyCount);
deckLines.push("");

for (const entry of energy) {
  const card = findCard(entry.name, entry.set, entry.num);
  if (card) { deckLines.push(ptcgoLine(entry.qty, card)); totalCards += entry.qty; }
}

deckLines.push("");
deckLines.push("Total Cards - " + totalCards);
deckLines.push("");
deckLines.push("****** Deck List Generated by SimpleDex ******");
deckLines.push("");

const output = deckLines.join("\n");
console.log(output);

const outPath = path.join(__dirname, "..", "decklists", "WC-2025-JP-Raging-Bolt.txt");
fs.mkdirSync(path.dirname(outPath), { recursive: true });
fs.writeFileSync(outPath, output, "utf8");
console.log("\nSaved to: " + outPath);