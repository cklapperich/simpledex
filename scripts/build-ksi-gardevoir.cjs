#!/usr/bin/env node
const path = require("path");
const fs = require("fs");
const cards = require(path.join(__dirname, "..", "dist", "cards-western.json"));

function findCard(name, setName, number) {
  const match = cards.find(c => {
    const cardName = c.names?.en || c.name || "";
    return cardName === name && c.set === setName && c.number === String(number);
  });
  if (!match) {
    console.error("WARNING: Could not find card: " + name + " | " + setName + " | " + number);
    return null;
  }
  return match;
}

function ptcgoLine(qty, card) {
  const name = card.names?.en || card.name || "???";
  return "* " + qty + " " + name + " " + card.set + " " + card.number;
}

const pokemon = [
  { qty: 3, name: "Ralts",          set: "Scarlet & Violet",    num: "084" },
  { qty: 2, name: "Kirlia",         set: "Scarlet & Violet",    num: "085" },
  { qty: 2, name: "Gardevoir ex",   set: "Scarlet & Violet",    num: "086" },
  { qty: 4, name: "Munkidori",      set: "Twilight Masquerade", num: "095" },
  { qty: 1, name: "Fezandipiti ex", set: "Shrouded Fable",     num: "038" },
  { qty: 1, name: "Flutter Mane",   set: "Temporal Forces",     num: "078" },
  { qty: 1, name: "Mew ex",         set: "151",                 num: "151" },
  { qty: 1, name: "Scream Tail",    set: "Paradox Rift",        num: "086" },
  { qty: 1, name: "Lillie" + String.fromCharCode(39) + "s Clefairy ex", set: "Journey Together", num: "056" },
];

const trainers = [
  { qty: 4, name: "Iono",                              set: "Paldea Evolved",      num: "185" },
  { qty: 4, name: "Professor" + String.fromCharCode(39) + "s Research",  set: "Scarlet & Violet",    num: "189" },
  { qty: 2, name: "Arven",                             set: "Scarlet & Violet",    num: "166" },
  { qty: 2, name: "Artazon",                           set: "Paldea Evolved",      num: "171" },
  { qty: 4, name: "Ultra Ball",                        set: "Scarlet & Violet",    num: "196" },
  { qty: 3, name: "Earthen Vessel",                    set: "Paradox Rift",        num: "163" },
  { qty: 2, name: "Counter Catcher",                   set: "Paradox Rift",        num: "160" },
  { qty: 2, name: "Nest Ball",                         set: "Scarlet & Violet",    num: "181" },
  { qty: 2, name: "Night Stretcher",                   set: "Shrouded Fable",      num: "061" },
  { qty: 2, name: "Rare Candy",                        set: "Scarlet & Violet",    num: "191" },
  { qty: 1, name: "Secret Box",                        set: "Twilight Masquerade", num: "163" },
  { qty: 1, name: "Super Rod",                         set: "Paldea Evolved",      num: "188" },
  { qty: 2, name: "Technical Machine: Evolution",      set: "Paradox Rift",        num: "178" },
  { qty: 1, name: "Bravery Charm",                     set: "Paldea Evolved",      num: "173" },
  { qty: 1, name: "Technical Machine: Devolution",     set: "Paradox Rift",        num: "177" },
];

const energy = [
  { qty: 7, name: "Basic Psychic Energy",  set: "Scarlet & Violet Energies", num: "5" },
  { qty: 4, name: "Basic Darkness Energy", set: "Scarlet & Violet Energies", num: "7" },
];

const deckLines = [];
let totalCards = 0;
let pokemonCount = pokemon.reduce((s, e) => s + e.qty, 0);
let trainerCount = trainers.reduce((s, e) => s + e.qty, 0);
let energyCount = energy.reduce((s, e) => s + e.qty, 0);

deckLines.push("****** Pokémon Trading Card Game Deck List ******");
deckLines.push("");
deckLines.push("##Pokémon - " + pokemonCount);
deckLines.push("");

for (const entry of pokemon) {
  const card = findCard(entry.name, entry.set, entry.num);
  if (card) { deckLines.push(ptcgoLine(entry.qty, card)); totalCards += entry.qty; }
}

deckLines.push("");
deckLines.push("##Trainer Cards - " + trainerCount);
deckLines.push("");

for (const entry of trainers) {
  const card = findCard(entry.name, entry.set, entry.num);
  if (card) { deckLines.push(ptcgoLine(entry.qty, card)); totalCards += entry.qty; }
}

deckLines.push("");
deckLines.push("##Energy - " + energyCount);
deckLines.push("");

for (const entry of energy) {
  const card = findCard(entry.name, entry.set, entry.num);
  if (card) { deckLines.push(ptcgoLine(entry.qty, card)); totalCards += entry.qty; }
}

deckLines.push("");
deckLines.push("Total Cards - " + totalCards);
deckLines.push("");
deckLines.push("****** Deck List Generated by SimpleDex ******");
deckLines.push("");

const output = deckLines.join("\n");
console.log(output);

const outPath = path.join(__dirname, "..", "decklists", "WC-2025-KSIs-Gardevoir.txt");
fs.mkdirSync(path.dirname(outPath), { recursive: true });
fs.writeFileSync(outPath, output, "utf8");
console.log("\nSaved to: " + outPath);